{
  "name": "github-actions-final-challenge",
  "version": "1.0.0",
  "private": true,
  "description": "Starter Node.js API for the GitHub + Actions + Docker final challenge",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "test": "node --test",
    "lint": "eslint ."
  },
  "dependencies": {
    "dotenv": "17.4.2",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  }
}
